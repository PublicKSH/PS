-- 코드를 입력하세요
SELECT ROUND(DATE_FORMAT(DATETIME, '%k'),0) AS HOUR, COUNT(*) AS COUNT
FROM ANIMAL_OUTS 
WHERE ROUND(DATE_FORMAT(DATETIME, '%k'),0) BETWEEN 9 AND 20
GROUP BY DATE_FORMAT(DATETIME, '%k')
ORDER BY HOUR ASC